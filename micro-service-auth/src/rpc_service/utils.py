class AuxiliaryLoginInterface:
    """
    Simulation of values from the received service data,
    if module <register_service.proto> were module <register.py>.
    from rpc_service.register.proto import UserRegisterCredentials
    """
    email: str
    password: str
